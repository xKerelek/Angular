import { FirstLetter } from './first-letter';

describe('FirstLetter', () => {
  it('should create an instance', () => {
    const directive = new FirstLetter();
    expect(directive).toBeTruthy();
  });
});
